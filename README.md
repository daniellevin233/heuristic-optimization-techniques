To set up the environment run:

```shell
/bin/bash ./setup.sh
```

To create a zip archive for submission run:

```shell
zip -r submission_assignment_1_DanielLevin_KartikArya.zip ./ -x "Supplementary\ Material/*" -x "venv/*" -x "*.lprof" -x "instances/*" -x "*.zip" -x ".git/*" -x ".idea/*" -x "*__pycache__/*"
```

```shell
zip -r submission_assignment_2_DanielLevin.zip ./ -x "Supplementary\ Material/*" -x "venv/*" -x "*.lprof" -x "instances/*" -x "*.zip" -x ".git/*" -x ".idea/*" -x "*__pycache__/*" -x "out/*" -x "exercises/*" -x "tuning_lecture.txt"
```
